<seccion>
  <table>
  <tr>
    <th>Id</th>
    <th>Nombre</th>
    <th>Descripci&oacute;n</th>
    <th>Estado</th>
    <th></th>
  </tr>

  <% @collection_types.each do |collection_type| %>
  <tr>
    <td><%= collection_type.id %></td>
    <td><%= collection_type.name %></td>
    <td><%= collection_type.description %></td>
    <td><% if collection_type.state %>Activo<% else %>Inactivo<% end %></td>
    <td><%= link_to 'Ver', collection_type %></td>
  </tr>
  <% end %>
  </table>
</seccion>

<botones>
  <%= link_to 'Crear Tipo de Cobranza', new_collection_type_path %>
  <%= render 'shared/pp_adm_dsg' %>
</botones>

