<seccion>
  <table>
  <tr>
    <th>Id</th>
    <th>Nombre</th>
    <th>Descripci&oacute;n</th>
    <th>Estado</th>
    <th></th>
  </tr>

  <% @payment_forms.each do |payment_form| %>
  <tr>
    <td><%= payment_form.id %></td>
    <td><%= payment_form.name %></td>
    <td><%= payment_form.description %></td>
    <td><% if payment_form.state %>Activo<% else %>Inactivo<% end %></td>
    <td><%= link_to 'Ver', payment_form %></td>
  </tr>
  <% end %>
  </table>
</seccion>

<botones>
  <%= link_to 'Crear Forma de Pago', new_payment_form_path %>
  <%= render 'shared/pp_adm_dsg' %>
</botones>

