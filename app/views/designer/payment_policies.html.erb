<div class="seccion">
  <table>
  <tr>
    <th><%= sortable "id" %></th>
    <th><%= sortable "principal_id", "Mandante" %></th>
    <th><%= sortable "product_id", "Producto" %></th>
    <th><%= sortable "collection_type_id", "Tipo de Cobranza" %></th>
    <th><%= sortable "state", "Estado" %></th>
    <th colspan=2></th>
  </tr>
  <% @payment_policies.each do |a| %>
  <tr>
    <td><%= a.id %></td>
    <td><%= a.principal_id %></td>
    <td><%= a.product_id %></td>
    <td><%= a.collection_type_id %></td>
    <td><%= a.state %>
    <td><%= link_to 'Ver', a %></td>
    <td><%= link_to 'Eliminar', :controller => 'designer', :action => 'del_reg', :id => a, :reg => 'payment_policy' %>
  </tr>
  <% end %>
  </table>
</div>
<div class="botones">
  <%= link_to 'Página Principal', :controller => "sessions", :action => "dsg" %>
</div>
