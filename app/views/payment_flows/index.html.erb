<div class="seccion">
  <table class="index">
  <tr>
    <th>Id</th>
    <th>Nombre</th>
    <th>Descripci&oacute;n</th>
    <th>Estado</th>
    <th></th>
  </tr>
  <% @payment_flows.each do |payment_flow| %>
  <tr>
    <td><%= payment_flow.id %></td>
    <td><%= payment_flow.name %></td>
    <td><%= payment_flow.description %></td>
    <td><% if payment_flow.state %>Activo<% else %>Inactivo<% end %></td>
    <td><%= link_to 'Ver', payment_flow %></td>
  </tr>
  <% end %>
  </table>
</div>
<div class="botones">
  <% if current_user.profile_id==8 %> 
    <%= link_to 'Crear Flujo de Pago', new_payment_flow_path %>
  <% end %>
  <%= render 'shared/pp_adm_dsg' %>
</div>

