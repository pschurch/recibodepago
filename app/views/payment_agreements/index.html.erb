<div class="seccion">
  <table>
  <tr>
    <th>Id</th>
    <th>Nombre</th>
    <th>Flujo Pago</th>
    <th>Descripci&oacute;n</th>
    <th>Estado</th>
    <th></th>
  </tr>
  <% @payment_agreements.each do |payment_agreement| %>
  <tr>
    <td><%= payment_agreement.id %></td>
    <td><%= payment_agreement.name %></td>
    <td><%= payment_agreement.payment_flow.name %></td>
    <td><%= payment_agreement.description %></td>
    <td><% if payment_agreement.state %>Activo<% else %>Inactivo<% end %></td>
    <td><%= link_to 'Ver', payment_agreement %></td>
  </tr>
  <% end %>
  </table>
</div>

<div class="botones">
  <%= link_to 'Crear Acuerdo de Pago', new_payment_agreement_path %>
  <%= render 'shared/pp_adm_dsg' %>
</div>

