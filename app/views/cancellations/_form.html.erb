<%= form_for(@cancellation) do |f| %>
  <% if @cancellation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cancellation.errors.count, "error") %> prohibited this cancellation from being saved:</h2>

      <ul>
      <% @cancellation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :state %><br />
    <%= f.check_box :state %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
