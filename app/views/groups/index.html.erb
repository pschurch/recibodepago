<div class="seccion">
  <table class="index">
  <tr>
    <th>Id</th>
    <th>Nombre</th>
    <th>Descripci&oacute;n</th>
    <th>Estado</th>
    <th></th>
  </tr>
  <% @groups.each do |group| %>
  <tr>
    <td><%= group.id %></td>
    <td><%= group.name %></td>
    <td><%= group.description %></td>
    <td><% if group.state %>Activo<% else %>Inactivo<% end %></td>
    <td><%= link_to 'Ver', group %></td>
  </tr>
  <% end %>
  </table>
</div>

<div class="botones">
  <%= link_to 'Crear Grupo', new_group_path %>
  <%= render 'shared/pp_adm_dsg' %>
</div>

