<div class="seccion">
  <div class="subtit">
    Recibos de Pago Rechazados por Supervisor, en espera a ser Aceptados:
  </div>
<table class="index">
  <tr>
    <th>Preparado Por</th>
    <th>N° RP</th>
    <th>Rut</th>
    <th>Identificador</th>
    <th>Mandante</th>
    <th>Producto</th>
    <th>Fecha</th>
    <th>T.Pagado</th>
    <th>selecc.</th>
    <th></th>
  </tr>
  <% if @receipts1.empty? %> 
    <tr><td colspan=10>No se han encontrado Recibos de Pago para ser Aceptados.</td></tr>
  <% else %>
  <% @receipts1.each do |receipt| %>
  <tr>
    <td><%= receipt.user_name %></td>
    <td><%= receipt.id %></td>
    <td></td>
    <td></td>
    <td></td>
    <td></td>
    <td></td>
    <td></td>
    <td></td>
    <td><%= link_to 'Ver', receipt %></td>
  </tr>
  <% end %>
<% end %>
</table>

  <div class="subtit">
    &nbsp;<p>Recibos de Pago Rechazados por Supervisor, Aceptados por Ejecutivo Cobranza:
  </div>
<table class="index">
  <tr>
    <th>Preparado Por</th>
    <th>N° RP</th>
    <th>Rut</th>
    <th>Identificador</th>
    <th>Mandante</th>
    <th>Producto</th>
    <th>Fecha</th>
    <th>T.Pagado</th>
    <th colspan=2></th>
  </tr>
  <% if @receipts2.empty? %> 
    <tr><td colspan=10>No se han encontrado Recibos de Pago Rechazados por Supervisor y Aceptados.</td></tr>
  <% else %>
  <% @receipts2.each do |receipt| %>
  <tr>
    <td><%= receipt.user_name %></td>
    <td><%= receipt.id %></td>
    <td></td>
    <td></td>
    <td></td>
    <td></td>
    <td></td>
    <td></td>
    <td><%= link_to 'Ver', receipt %></td>
    <td><%= link_to 'Editar', edit_receipt_path(receipt) %></td>
  </tr>
  <% end %>
<% end %>
</table>

</div>

<div class="botones">
  <%= render 'shared/pp_ejc_dsg' %>
</div>

