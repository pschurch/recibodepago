<seccion>
  <table>
  <tr>
    <th>Id</th>
    <th>Nombre</th>
    <th>Descripci&oacute;n</th>
    <th>Estado</th>
    <th></th>
  </tr>

  <% @products.each do |product| %>
  <tr>
    <td><%= product.id %></td>
    <td><%= product.name %></td>
    <td><%= product.description %></td>
    <td><% if product.state %>Activo<% else %>Inactivo<% end %></td>
    <td><%= link_to 'Ver', product %></td>
  </tr>
  <% end %>
  </table>
</seccion>

<botones>
  <%= link_to 'Crear Producto', new_product_path %>
  <%= link_to 'Página Principal', :controller => "sessions", :action => "adm" %>
</botones>
